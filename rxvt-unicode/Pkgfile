# Description: rxvt-unicode is a customizable terminal emulator forked from rxvt
# URL: http://mhash.sourceforge.net/
# Maintainer: samsep10l at dark_bakura at protonmail dot com
# Depends on: libptytty
# Optional:

name=rxvt-unicode
version=9.30
release=1
source=(http://dist.schmorp.de/rxvt-unicode/rxvt-unicode-$version.tar.bz2)

build() {
    cd $name-$version

    ## Credits to:
    ## https://github.com/janvkn/rxvt-unicode-custom/blob/master/PKGBUILD

    ./configure \
        --prefix=/usr \
        --libdir=/usr/lib \
        --mandir=/usr/share/man \
        --enable-256-color \
        --enable-combining \
        --enable-fading \
        --enable-font-styles \
        --enable-iso14755 \
        --enable-keepscrolling \
        --enable-lastlog \
        --enable-mousewheel \
        --enable-next-scroll \
        --enable-perl \
        --enable-pointer-blank \
        --enable-rxvt-scroll \
        --enable-selectionscrolling \
        --enable-slipwheeling \
        --enable-smart-resize \
        --disable-startup-notifications \
        --enable-transparency \
        --enable-unicode3 \
        --enable-utmp \
        --enable-wtmp \
        --enable-xft \
        --enable-xim \
        --enable-xterm-scroll

    make
    make DESTDIR=$PKG install
}
