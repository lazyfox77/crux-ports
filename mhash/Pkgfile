# Description: A hash algorithm library
# URL: http://mhash.sourceforge.net/
# Maintainer: samsep10l at dark_bakura at protonmail dot com
# Depends on:
# Optional:

name=mhash
version=0.9.9.9
release=1
source=(https://downloads.sourceforge.net/sourceforge/mhash/$name-${version}.tar.bz2)

build() {
    cd $name-$version

    ./configure \
        --prefix=/usr \
        --libdir=/usr/lib \
        --mandir=/usr/share/man
    make
    make DESTDIR=$PKG install
}
